pydantic>=2.0.0
pyyaml>=6.0
bcrypt>=4.0.0
openai>=1.0.0
google-generativeai>=0.8.0
boto3>=1.34.0
python-dotenv>=1.0.0
filelock>=3.13.0
fastapi>=0.111.0
uvicorn>=0.30.0
requests>=2.32.0
python-multipart>=0.0.9
pytest>=7.4.0
amazon-transcribe>=0.6.2
# flokiq_sync local mirror DB (services/flokiq_sync/) — MySQL driver only,
# no other flokiq_sync code touches the real sandbox DB.
pymysql>=1.1.0
# Nova Sonic (speech-to-speech, InvokeModelWithBidirectionalStream) test/eval
# path. Requires Python >=3.12 — the reason the app venv moved to 3.13.
aws_sdk_bedrock_runtime>=0.11.0
awscrt>=0.23.0
gtts>=2.5.0
# Orchestration-layer migration (see /Users/sudhanshu/.claude/plans/
# elegant-roaming-river.md) — [extensions] is required for the LiteLlm
# model wrapper that lets ADK agents call AWS Bedrock (this project's real
# model backend) instead of only Gemini. Verified working end-to-end
# against bedrock/deepseek.v3-v1:0 with this project's existing AWS
# credentials, 2026-09-20.
google-adk[extensions]>=2.9.2
